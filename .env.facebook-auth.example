# Facebook OAuth Configuration
# Add these variables to your .env file

# Facebook App ID from Facebook Developer Console
REACT_APP_FACEBOOK_APP_ID=your_facebook_app_id

# Facebook App Secret is not needed on client side, it's only used on server
# REACT_APP_API_URL should already be set in your .env file
# Example: REACT_APP_API_URL=http://localhost:5000
